CREATE TABLE fueltypes (_id INTEGER PRIMARY KEY AUTOINCREMENT, cars_id INTEGER NOT NULL, name TEXT NOT NULL, tank INTEGER NOT NULL, FOREIGN KEY(cars_id) REFERENCES cars(_id) ON UPDATE CASCADE ON DELETE CASCADE);